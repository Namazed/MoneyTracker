<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    style="@style/Container"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/transparent"
        android:orientation="vertical">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_marginLeft="@dimen/tvMarginListItem"
            android:textColor="@color/tvColorListItem"
            android:textSize="@dimen/tvSizeListItem"
            />

        <TextView
            android:id="@+id/date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_alignParentStart="true"
            android:layout_marginLeft="@dimen/tvMarginListItem"
            android:textColor="@color/tvColorListItem"
            android:textSize="@dimen/tvSizeListItem"
            />
    </LinearLayout>

    <TextView
        android:id="@+id/sum"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_marginRight="@dimen/tvMarginListItem"
        android:layout_marginTop="@dimen/tvMarginListItem"
        android:textColor="@color/tvColorListItem"
        android:textSize="@dimen/tvSizeListItem"
        />

</RelativeLayout>